# Server Configuration
NODE_ENV=development
PORT=5000

# Authentication / JWT
JWT_SECRET=nexuspos_dev_jwt_secret_key_2026
JWT_REFRESH_SECRET=nexuspos_dev_jwt_refresh_secret_key_2026

# Database Connection Details
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=NEXUSPOS
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/NEXUSPOS

# Database Pool Settings
DB_POOL_MAX=20
DB_IDLE_TIMEOUT=30000
DB_CONN_TIMEOUT=5000

# Initial Admin Credentials (used during seed)
ADMIN_INITIAL_PASSWORD=password
ADMIN_INITIAL_PIN=1234
